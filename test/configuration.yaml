default_config:

demo:

lovelace:
  mode: storage
  dashboards:
    lovelace-yaml:
      mode: yaml
      title: yaml
      filename: lovelace.yaml

input_select:
  background:
    name: Background
    options:
      - bear
      - kitten
input_number:
  x_pos:
    name: x
    min: 0
    max: 100
  y_pos:
    name: y
    min: 0
    max: 100

frontend:
  themes:
    test:
      card-mod-card: |
        ha-card {
          background: blue;
        }
      card-mod-row: |
        :host {
          display: block;
          background: {% if is_state(config.entity, 'on') %}green{% else %}red{% endif %};
        }
    test2:
      card-mod-card: |
        ha-card {
          background: green;
          }
